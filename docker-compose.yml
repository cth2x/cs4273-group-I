version: '3.9'

services:

  next:
    build:
      context: .
    ports:
      - 3000:3000
    volumes:
      - ./missing-persons/:/code
    command: npx next dev
  db:
    image: mysql
    restart: always
    port:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: testing
  
